sudo: required
dist: trusty

matrix:
  include:
    - language: java
      jdk: oraclejdk8
      before_install:
        - chmod +x mvnw
      before_script:
        - cd ./src/spring/

    - language: node_js
      node_js: stable
      install:
        - npm install
      before_install:
        - cd ./src/angular/
        - export DISPLAY=:99.0
        - sh -e /etc/init.d/xvfb start
      script:
        - npm run build